# Source this in shells and CI steps that run `crab`.
# Do not commit the real PSK value in repositories.

export CRAB_CACHE_SERVICE_URL="https://crab-cache.example.com:8443"
export CRAB_CACHE_PSK="replace-with-long-random-secret"
export RUST_LOG="${RUST_LOG:-info,crab::cache=debug}"
