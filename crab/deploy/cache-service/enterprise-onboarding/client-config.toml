# Crab client cache-service config.
#
# Install into ~/.crab/local.toml for user-wide rollout, or into
# <repo>/.crab/local.toml when one repository should opt in.

[cache]
service_url = "https://crab-cache.example.com:8443"
service_mode = "cache+dedup"
service_auth = "psk"
push_warming = true
