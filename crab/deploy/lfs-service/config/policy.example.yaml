# Repository-scoped permissions for crab-lfs-server.
# Copy to the configured policy_path and keep it outside the image.
rules:
  - principal: operator
    repos:
      - team/*
    actions:
      - read
      - write
      - admin
