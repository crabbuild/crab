{{- if .Values.networkPolicy.enabled }}
{{- include "crab-http-server.validateNetworkPeers" (dict "name" "networkPolicy.publicIngressFrom" "peers" .Values.networkPolicy.publicIngressFrom) }}
{{- include "crab-http-server.validateNetworkPeers" (dict "name" "networkPolicy.metricsIngressFrom" "peers" .Values.networkPolicy.metricsIngressFrom) }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "crab-http-server.fullname" . }}
  labels:
    {{- include "crab-http-server.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "crab-http-server.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
  {{- if or .Values.networkPolicy.publicIngressFrom .Values.networkPolicy.metricsIngressFrom }}
  ingress:
    {{- with .Values.networkPolicy.publicIngressFrom }}
    - ports:
        - port: http
          protocol: TCP
      from:
        {{- toYaml . | nindent 8 }}
    {{- end }}
    {{- with .Values.networkPolicy.metricsIngressFrom }}
    - ports:
        - port: management
          protocol: TCP
      from:
        {{- toYaml . | nindent 8 }}
    {{- end }}
  {{- else }}
  ingress: []
  {{- end }}
{{- end }}
