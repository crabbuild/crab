[package]
name = "crab-ltx-perf-crab"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]
resolver = "2"

[dependencies]
crab-ltx = { path = "../../", default-features = false }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tempfile = "3"
