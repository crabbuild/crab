# Current GC Hardening Roadmap

## Compatibility policy

The hard-cut policy below governed the pre-launch work recorded in Plans
001-016. Crab now has tagged 1.0 releases, so it applies only to unshipped
behavior and explicitly disposable local development state. Tagged repository
formats, object layouts, APIs, configuration, and structured output require a
compatible reader, an explicit migration/refusal boundary, or an approved
breaking-release policy. Plan 017 owns the post-launch contract inventory and
release evidence.

All surviving pre-launch Crab-owned product and serialized contracts were
named **v1**. External protocol names such as Git protocol v2 were unaffected.
That work hard-cut to the best canonical shape: it replaced readers/writers in
place, deleted obsolete branches, fixtures, aliases, migrations, dual
reads/writes, and rollback-only code, and required regeneration, restaging, or
reinitialization for disposable development state. Do not apply that historical
rule to reset repositories written by a tagged release.

Breaking changes must earn the cutover through a simpler canonical path or a
demonstrable correctness, durability, security, or performance improvement.
This policy permits deep redesign; it does not justify gratuitous churn.

This policy supersedes older plan text that assumes descriptor-less fallback,
data migration, rollback windows, or simultaneous old/new format support.
Before executing an older plan, reconcile it to this rule. Plans 002 and 003
are now reconciled. `010-migration-provider-rollout.md` was deleted and replaced by
`010-provider-qualification-v1-cutover.md`.

Generated by the `improve` skill on 2026-08-22 from commit `b738f3b2`.
This is a standalone delivery track for the GC implementation that exists in
the current Unified repository layout. It does not depend on the PB plans under
`plans/001-pb-contract-and-baselines.md` through
`plans/010-provider-qualification-v1-cutover.md`; those plans describe behavior
that is not an input to this track.

## Current implementation boundary

- Repo GC owns repo-local `packs/`, `metadata/`, `manifests/`, workflow
  artifacts, stage/experiment roots, and workflow cache xorbs
  (`crab/src/cmd/gc/mod.rs:39-51`).
- Shared `.crab/shards/` and `.crab/xorbs/` are bucket-owned. Bucket GC uses the
  current `.crab/ref-registry`, current and historical manifests, coordinator
  protection, adaptive two-hex listing, strict immutable shard closures, and
  generation-pinned file-index cleanup (`crab/src/cmd/gc/bucket.rs:129-510`).
- Service ACL views can use a view-local `<repo>/.crab` global prefix through
  `crates/crab-auth-server/src/view.rs:602-610`; that prefix is outside the
  current bucket-GC `.crab` scope and must not be silently folded into this
  track without its own roots and deletion contract.
- Direct pushes already acquire weighted `PushAdmissionTicket` capacity and
  keep it until canonical commit (`crab/src/git/push.rs:15179-15235`,
  `crab/src/git/push.rs:5218-5270`). Protected pushes deliberately rely on
  authenticated service admission; active-active pushes rely on the write
  coordinator. Do not replace those ownership boundaries with a client-only
  lock.
- Destructive GC now holds a renewable exclusive `GcSweepLease` across the
  full mark/plan/delete/reconcile interval. Canonical writers acquire shared
  global+repo `GcWriterLeases` before publication (`crab/src/maintenance.rs`,
  `crab/src/cmd/gc/{mod,bucket}.rs`).
- The CLI bucket path derives active-active protected keys only after that
  bucket sweep fence is held; durable journal batches also reject keys outside
  their scope namespace, and closure publication is content-addressed and
  idempotent.
- Active-active repo GC now acquires a coordinator fence and validates its
  safety snapshot; bucket GC still requires a per-repo coordinator proof but
  does not yet drain every external coordinator transaction.

## Remaining adoption blockers after this implementation pass

1. Bucket and repo destructive execution now journal bounded candidate batches
   and stream provider prefixes and reachability roots into run-owned marks.
   Pack-list records are consumed one at a time and store-only deletion no
   longer retains a process-wide deleted-key list. The current repository
   snapshot/ref-journal materializer, workflow artifact registry, bucket
   dry-run/repair paths, and closure sidecars still have bounded-but-materialized
   joins; those are the remaining memory-scaling items.
2. Planning interruption is recoverable: an unsealed run is reset and replayed
   from the original snapshot after root/policy validation. The replay is
   deterministic but currently restarts source enumeration rather than storing
   provider cursors.
3. Closure sidecars are strict, size-checked, and missing coverage fails
   closed. Closure reads are limited to a small body budget. The existing
   `--repair-closures` path is obsolete under the v1 hard cutover and must be
   deleted; canonical repositories require complete closure coverage from
   genesis. A sidecar is still a single JSON vector, so very large closures
   fail closed instead of being segmented.
4. Provider reports are cost-analysis inputs, not deletion proofs. A strict
   inventory contract and fail-closed gate now exist, but the current
   S3/GCS/Azure CSV readers still skip malformed rows and coerce invalid sizes to zero;
   the GCS CSV reader labels its result as Parquet
   (`crab/src/cost/inventory/report/s3.rs:31-108`,
   `crab/src/cost/inventory/report/gcs.rs:31-91`,
   `crab/src/cost/inventory/report/azure.rs:29-92`). No GC path consumes them.
5. Qualification now has deterministic fixture/evidence generation and a
   validator, but no retained GC race, crash-resume, high-cardinality bounded-
   memory, or real-provider GC pass has been recorded yet
   (`crab/scripts/e2e/gc_qualification.py`,
   `crab/scripts/e2e/validate_gc_evidence.py`).
6. The CLI now resolves omitted `--grace-period` from `Config::gc_grace_period`
   (24 hours by default); explicit values still work and non-force runs retain
   the one-hour minimum clamp. Provider inventory and real-scale qualification
   remain open.

## Execution order

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| 001 | Close the current writer/GC fence | P1 | L | — | IMPLEMENTED (focused proof) |
| 002 | Replace in-memory sweep with a durable bounded run engine | P1 | L | 001 | PARTIAL (destructive roots/candidates stream; snapshot and preview joins open) |
| 003 | Persist and consume complete shard closures | P1 | L | 001, 002, add/push 013 | REWORK (delete repair/backfill; require canonical v1 genesis coverage) |
| 004 | Add strict provider inventory adapters as an optional candidate source | P2 | L | 002, 003 | PARTIAL (contract/gate; adapters open) |
| 005 | Qualify GC under real races, crashes, scale, and providers | P1 | L | 001–003; 004 for inventory mode | PARTIAL (harness/validator; evidence open) |

Plan 001 is independently deliverable and must land before optimization work;
the implementation is safe for the current writer map but still needs the
complete writer-inventory and race qualification gates below.
Plan 004 is not required for live-list GC; it becomes a second candidate-source
adapter only after the run engine is durable. Plan 005 is the release gate and
can qualify live-list GC after Plans 001–003; its inventory-provider rows are
gated on Plan 004.

## Shared invariants

- Never delete a referenced current or historical manifest root, ref-journal
  object, workflow/artifact root, coordinator-protected object, grace-period
  object, or object covered by an unresolved writer lease.
- `--force` bypasses age only. It never bypasses admission, root revalidation,
  registry completeness, coordinator fencing, closure coverage, or a failed
  inventory/run journal.
- Object-store state is the correctness source of truth. Local temporary files
  may bound sorting, but process death must be recoverable from the durable run
  journal and must not require a particular filesystem.
- Every SlateDB/MetaDb guard opened by GC or canonical closure publication closes on success,
  cancellation, and error. Every lease/heartbeat is released or its loss fails
  the run closed.

## Standalone release acceptance (not yet met)

- Direct, protected, active-active, repack, restripe, recovery, replication,
  workflow, and artifact writers are fenced in the current paths; the writer
  inventory and injected race proof are still required before release.
- Root drift, coordinator epoch drift, lease loss, cancellation, process death,
  transient delete failure, and partial provider listing all fail closed or
  resume idempotently with one durable outcome per candidate.
- Peak RAM, temporary bytes, open files, and provider requests stay within
  explicit budgets for a namespace at least 10× larger than available RAM.
- Complete closure coverage yields zero referenced-shard body GETs; missing or
  corrupt closure coverage refuses destructive bucket GC. Orphan closure
  sidecars are now considered bucket candidates only when their source shard is
  absent or in the same expired shard deletion set.
- Inventory-backed runs require one fresh, complete, scope-matched, strictly
  parsed report identity and perform no recursive LIST.
- Post-run fsck and fresh clone/hydrate prove byte-identical data.
- CLI help, config, JSON/JSONL output, architecture docs, web docs, and
  recovery/runbooks describe one grace/force/plan/resume contract.

The current draft integration branch assembles the Phase 1-5 contracts for
end-to-end review. This does not override the dependency graph or acceptance
gates: `001-large-repository-scale-roadmap.md` records which implementation is
present and which Kubernetes/RustFS, fault, provider, and canary evidence is
still pending.

## Findings considered and rejected

- Raising grace alone: it cannot protect a writer longer than grace and is
  explicitly bypassed by current `--force`.
- Increasing LIST/DELETE concurrency: it raises provider pressure without
  bounding memory or making partial deletion resumable.
- Holding `repository-maintenance` for an entire long push or GC run: correct
  but an avoidable writer outage; use a renewable bounded sweep fence.
- Reusing the cost inventory `Inventory` type for destructive authorization:
  its permissive skip/zero semantics are appropriate for estimates, not deletes.
- Importing PB layout/recipe/partition assumptions: they are not current GC
  roots and would make this delivery untestable against the current store.

## Large-repository scale track

`001-large-repository-scale-roadmap.md` is a separate P1, high-risk delivery
track for Kubernetes-scale Git history and team fanout. It covers bitmap
reachability, delta-preserving response packs, a generation-bound object
catalog, split commit graphs, bounded geometric maintenance, stale-pack
retention, indexed stale-pack cleanup, and concurrency qualification. Its
implementation includes real Kubernetes/RustFS replay, clone, shallow-fetch,
and 100-client coordination proof; the roadmap records those completed gates
separately from the pending 10,000-push, fault, provider, retention, failover,
and canary acceptance evidence.

## Post-launch product hardening track

[`017-post-launch-product-hardening.md`](017-post-launch-product-hardening.md)
is the P0 product roadmap after Crab 1.0.1. It orders the remaining work into
five priorities: authoritative integrity/recovery, native Git and mirror
correctness, metadata/maintenance scale, performance/cloud-cost SLOs, and team
adoption through the managed control plane. It reuses this GC track and the
large-repository roadmap as detailed owners, and adds a mandatory compatibility
and exact-release-evidence foundation.

Status: **IN PROGRESS** — Phase 2 implementation and local proof are recorded;
full Phase 2 acceptance and the other phase outcomes remain open. The plan
includes accountable roles, first execution packets, dependency gates and
decisions required before delivery estimates. Product rationale, issue-ready
ticket fields, verification entry points and managed-stage acceptance gates
make the roadmap executable without treating all five priorities as one release.
Existing implementation does not
satisfy Plan 017 merely because a subsystem's code or unit tests exist; each
claimed row needs its own acceptance criteria and retained release-shaped
evidence.

## Add/push hardening track

Generated by the `improve` workflow on 2026-08-29 from commit `86139f55` and
extended at commit `d7d0ee8e`.
This track hardens the current add/staging/push/shard layout. It is independent
of the GC track above. Execute correctness and lifecycle ownership before
throughput optimization; every executor must read its plan fully, honor STOP
conditions, and update the status row.

Every plan in this track follows the global cutover policy: no v2 format, no
backward compatibility, and no migration of disposable pre-user state.

### Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| [011](011-transactional-add-publication.md) | Make add publication recoverable and prepared-aware | P1 | L | — | DONE (focused, broad, E2E, and RustFS proof) |
| [012](012-canonical-path-lease-lifecycle.md) | Give each staged path one canonical lease lifecycle | P1 | L | 011 | DONE (focused, broad, scale-proxy, and RustFS proof) |
| [013](013-dependency-closed-shard-partitioning.md) | Partition shards as dependency-closed file bundles | P1 | L | — | DONE (focused, property, protected-view, and broad proof) |
| [014](014-retain-new-xorb-metadata-across-replan.md) | Retain full new-xorb metadata across CAS replans | P1 | M | 013 | DONE (real shared-xorb CAS regression and broad proof) |
| [015](015-bound-prepared-xorb-push.md) | Bound prepared-xorb push work per file and source xorb | P1 | L | 011 | DONE (counter, E2E, multipart, and broad proof) |
| [016](016-canonical-prepared-chunk-authority.md) | Give each prepared chunk one canonical local authority | P1 | L | 011, 012, 015 | DONE (property, full E2E, and fresh-bucket RustFS proof) |

Status values for this track: `TODO`, `IN PROGRESS`, `DONE`, `BLOCKED`
(with a one-line reason), or `REJECTED` (with a one-line rationale).

### Dependency notes

- 011 establishes the only recoverable Git-index/staging publication boundary
  and the prepared-aware promotion path. 012 and 015 must build on that
  canonical ownership rather than adding another lookup or fallback.
- 012 may proceed in parallel with 013 after 011 lands.
- 013 establishes the shard file-bundle contract. 014 then supplies complete
  shared-xorb metadata through that contract during non-atomic CAS replans.
- 015 was deliberately sequenced after the original lifecycle work because its
  performance refactor needed a stable ownership contract. It could run in
  parallel with 013/014 once 011 was complete if the branches coordinated
  `push.rs` edits.
- 016 preserves 011's publication boundary, 012's path/lease/snapshot
  lifecycle, and 015's bounded prepared reads/uploads, but hard-cuts their
  per-file prepared-plan representation to one preparation-wide normalized
  authority. It is the remaining add/push correctness gate.

### Release gates

- A direct prepared `crab add --skip-git-add` followed by ordinary `git add`
  succeeds without segment duplication, preserves every earlier committed
  version through first push, leaves no superseded open batch, and an injected
  crash after Git index replacement reconciles to one published transaction.
- Restaging one path repeatedly retains bytes proportional to current path
  heads, committed-but-unpushed history owners, and open push pins, not
  uncommitted edit history.
- Forced multi-shard direct and protected-view builds independently extract
  every file recipe from its containing shard.
- A non-atomic CAS conflict with two refs sharing one newly uploaded xorb
  commits the unconflicted ref without reupload or sparse-index failure.
- Mixed prepared/no-plan pushes read each unique prepared source xorb at most
  once per attempt and stream protected file-backed payloads without full-body
  materialization.
- Two or more directly streamed files with partial chunk overlap produce one
  local prepared placement per shared hash, push each missing xorb once, and
  clone/hydrate byte-identically on RustFS.
- Focused tests pass locally with
  `CARGO_TARGET_DIR=/Volumes/Workspace/crabbuild-target/crab-main`; broad suites,
  E2E smoke, and provider qualification run in CI/dedicated environments.

### Delivered outcome and evidence

| Area | Canonical delivered behavior | Primary proof |
|---|---|---|
| `crab add` | One durable publication intent atomically binds exact staged recipes to the Git index replacement. | `crab/src/cmd/add.rs`; `publication_intent_atomically_replaces_canonical_path_head`; publication reconciliation tests |
| Prepared xorbs | Unknown bytes are claimed preparation-wide and direct-packed once without a duplicate segment payload. Remote hits create neither local payload form. | `crates/crab-staging/src/stream.rs`; `direct_stream_partial_overlap_has_one_canonical_prepared_placement`; `proven_remote_chunks_create_no_local_payload_copy` |
| Remote duplicates | Origin- and placement-bound remote proof produces no local payload, prepared xorb, or upload. | `ExistingChunkCandidate`; exact committed-placement revalidation; RustFS run `codex-cross-repo-dedup-release-20260829-5` |
| Re-staging | One path head is replaced atomically; unowned superseded authority is reclaimed. | `repeated_path_publication_keeps_one_head_and_one_ordinary_lease`; `restaging_path_reclaims_prepared_payload_after_snapshot_release` |
| Concurrent push/restage | Push pins an immutable recipe snapshot while a newer path head becomes current. | `open_push_snapshot_pins_superseded_path_until_retirement`; `committed_recipe_survives_same_path_restage_until_push` |
| Push planning | Prepared plans are validated and adopted per file; a missing or stale sibling falls back independently. | `mixed_add_time_push_plan_keeps_valid_sibling_and_packs_only_missing_plan` |
| Prepared reads | Residual requests coalesce by source xorb; each complete source is opened and verified once under a byte-aware bound. | `prepared_residual_plan_reads_each_unique_source_once` |
| Memory | Recipe pages hold at most 512 occurrences; remote proof pages, prepared reads, packing, and upload permits are bounded. | `RECIPE_PAGE_ENTRIES`; `recipe_remote_chunk_page`; staging scale proxies |
| Uploads | File-backed xorb bodies use retryable multipart upload through ordinary and protected staged stores. | `put_multipart_file_retry`; `protected_file_backed_xorb_streams_to_staging_store` |
| CAS retries | Complete new-xorb metadata survives ref-subset replanning and the immutable body is reused. | `shared_xorb_non_atomic_replan_cas_dependency_replan_reuses_one_upload` |
| Shards | Every file and all full xorb metadata required by its terms are emitted as one deterministic dependency-closed bundle. | `PushShardSession::add_file_bundle`; direct and protected-view forced-partition tests |
| Ref safety | Xorbs, shards, indexes, recipes, and Git objects are durable before manifest/ref publication. | push state-machine order; `metadb_commit_failure_aborts_before_refs_advance`; full remote-helper suite |
| Recovery | Add intents and push snapshots reconcile or retire idempotently after error, cancellation, or restart. | publication recovery, cancellation, snapshot recovery, and full E2E suites |
| Formats | Crab-owned contracts have one strict v1 reader/writer; non-v1 and incomplete state fail closed. | `v1_contract_inventory`; `workflow_v1_contracts`; retired migration tests removed |

Local S3-compatible qualification is retained as RustFS run
`codex-full-release-20260829-7`: 115/115 checks passed against bucket `beyond`,
including all three v1 cutover/add modes and fresh byte-identical hydration.
Plan 010 remains blocked on retained real AWS, GCS, and Azure evidence. The
full Rust test target passes; strict repo-wide Clippy still reports 449
warnings-as-errors across the monolithic CLI, while the focused shared crates
changed by this track pass their strict Clippy gates.

### Canonical prepared authority delivered

Plan 016 closes the direct-stream ownership gap. Per-file builders coordinate
through one disk-backed add preparation, each chunk hash has one canonical
prepared placement, local payloads are content-addressed, and recipes share
them through normalized leases. The bucket-global SlateDB remains committed-
receipt metadata only.

### Existing plans reused instead of duplicated

- `004-bounded-local-recipes.md` already owns paged immutable local recipe
  sequences and unbounded chunk-vector removal.
- `006-streaming-dedup-and-push.md` already owns add-time remote classification
  before full prepared packing.
- Shard proof receipts, incremental shard serialization/upload, and immediate
  xorb upload-receipt persistence remain follow-ups after 013 and 015 establish
  stable ownership/measurement boundaries.

### Findings considered and rejected for this track

- Make open batches generally push-visible: rejected; Git index/committed
  pointers remain the publication boundary.
- Weaken prepared payload or dense xorb metadata validation to avoid fallback:
  rejected; reconstruction integrity must fail closed.
- Teach exact-recipe consumers to scan arbitrary shards: rejected; it turns a
  producer defect into cross-shard read amplification and a broader contract.
- Treat same-size Crab status as byte equality: rejected for this track; the
  current size-based signal is documented and Git remains the change detector.
- Repair shard or xorb integrity from HEAD/cache identity alone: rejected;
  immutable bodies and origin-bound proofs remain hash-verified.
- Treat the unused `xorb_data_map` as duplicated payload memory: rejected; its
  values are empty. Plan 013 removes the smaller O(xorbs) dead allocation.
- Put provisional prepared ownership in bucket-global SlateDB: rejected; it
  would make add depend on distributed availability and advertise local bytes
  that other clients cannot read. SlateDB remains committed-receipt metadata.
- Disable multi-file direct preparation: rejected as the final design; it
  restores correctness by giving up one-pass packing and cross-add reuse.
- Let push pick one of two prepared placements: rejected; selection would be
  order-dependent and would conceal a producer invariant violation.
- Always retain both a segment and prepared body: rejected; direct preparation
  must not double local writes merely to manufacture a fallback.

## Local read/cache hardening track

Generated from a read/cache/dedup/hydration audit at commit `63bfc8c` on
2026-09-02. This is a standalone P1 track. It consumes the current v1
xorb/shard and committed-placement contracts but does not change their remote
formats or the authoritative add/push lifecycle from Plans 011-016.

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| [017](017-local-cache-read-hardening.md) | Harden local cache, deduplication, and hydration | P1 | XL | Current v1 layouts and chunk-index generation contract | IN PROGRESS (Phase 0 delivered) |

Plan 017 is split into eight phases, each delivered in bounded PRs: observable contract/docs, canonical
read runtime, cache-failure isolation, unified budget, private tenancy,
operability/state cleanup, concurrency/startup, and production qualification.
Each phase has its own context, acceptance criteria, proof, and STOP conditions.

Qualification tooling is now in progress: the maintained cache-service smoke
uses fresh dedicated buckets and create-only scoped fixtures. Report-audit
consolidation now delegates to the packaged verifier in source-tree and
retained-bundle layouts; both entry points pass. Stricter cold/warm/restart
evidence validation remains local; the incomplete Rust test fixture awaits an
approved input update. See Plan 017 for retained passes, failures, and the unresolved
shared-bucket fixture incident. The installed HTTP-503 outage workload now
proves add/push/dedup and cold/warm hydration, including a focused 128 MiB run;
slow-service bounds and versioned outage release-gate coverage remain open.
The latest installed candidate also fixes late cache-body failure escaping
metadata reads: anonymous budgeted completion restores origin fallback, proven
with a failing previous binary, a 128 MiB fault workload, and a full smoke.
The harness now lets Crab create private client roots; platform and resource
acceptance for response completion remain open.
Body completion is pushed in draft PR 147 as `04d271b`. Continued installed
128 MiB qualification passes byte identity under refused connections and
single header/body stalls, but each stall adds about 30 seconds; sustained
connection stalls exceed a 100-second harness deadline. Plan 017 retains the
failure and executable classification, suppression/recovery, and evidence
slices. The diagnostic pass is not an aggregate latency guarantee.
Follow-up `9dffbe6` shares service failure suppression/recovery across client
clones while preserving request-local errors and partial-batch dedup. The
installed candidate reduces failed metadata service attempts from 51 to one;
sustained-stall hydration completes in 30.4 seconds with verified bytes, and
the full 1,204-check RustFS smoke passes. Same-process push recovery now has
installed proof, and `0657359` retains timed-out command records. The
maintained recovery stage covers successful negotiation, one failed metadata
PUT, cooldown without requests, recovered cache HITs, and fresh clone/hydrate
byte identity. Its 128 MiB run passes; the pre-suppression binary fails the
cooldown gate. Versioned evidence enforcement, other read surfaces, resource
bounds, and the other phase acceptance items remain open. The existing CLI
warming test's outage expectation remains red pending approval; no assertion
was weakened. See Plan 017 for exact artifact/source identities and limits.
This is not phase acceptance or release proof.

### Release gates

- One reusable hydrator/store client serves fetch, explicit hydrate, smudge,
  clone auto-hydrate, mount, worktree, VFS, and authenticated reads.
- Fetch followed by disabled-origin explicit hydrate reconstructs identical
  bytes with zero xorb body GETs, without creating a duplicate full-xorb copy.
- Disposable cache corruption, permissions, schema, and write failures never
  block valid origin data; invalid origin data still fails closed.
- One effective root and one 10 GiB-by-default product budget cover every
  disposable local cache family.
- Cache contents are private to one OS user; diagnostics are complete,
  read-only when inspecting, and actionable.
- Multi-process fill and startup work are bounded, cancel-safe, and qualified
  on RustFS plus supported real providers and operating systems.

## Product-readiness coordination

[Plan 018: Close Crab's product-readiness gaps](018-product-readiness-roadmap.md)
coordinates the existing cache, write-durability, GC, provider, and scale
tracks. Status: DESIGN READY FOR REVIEW; implementation acceptance remains
open. It distinguishes current implementation from missing qualification and
does not replace the owning subsystem plans.

Its seven phases cover the support/evidence contract, local read/cache safety,
recovery and maintenance, provider/security boundaries, self-service
diagnostics, the resource envelope, and evidence-gated release/canary. Each
phase specifies context, work, dependencies, ownership, acceptance criteria,
proof, and STOP conditions. Plan 017's working-tree snapshot records partial
cache implementation separately from accepted phase delivery.
Plan 018 also includes a dependency-ordered kickoff queue and a per-phase
acceptance record. Start with the supported-journey/evidence inventory; private
filesystem access and live-state ownership precede destructive cache eviction.
Use Plan 018 for the product-wide priorities and release cuts; use Plan 017
for cache/read implementation details and its current evidence ledger. Both
distinguish partial implementation from acceptance. Disk capacity and read
memory are separate resource contracts. Source-preserving reconstruction now
has shared and CLI failure-path proof; remaining diagnostic classes and real
restore/service qualification remain open.
The current read-memory slice has fallible allocation, exact-length checks,
and cancellation ownership; configured admission and returned-result lifetime
accounting remain open. Plan 017 records the VFS startup/private-chunk-cache
repair and real warm-range proof separately from remaining file-window cache
ownership and native mount gates. Neither shared unit-test success nor a
zero-test feature selection qualifies mounted reads.
Object/range stats/prune/verify and targeted object eviction now share pinned
private scans and locked conditional deletion. Full-file xorb checks also
validate the footer payload digest. Plan 017 keeps database/root authority,
reservation coverage, effective ACLs, and native OS qualification
as explicit remaining slices; this is not complete cache-maintenance acceptance.

Current database checkpoint: Plan 017's descriptor-bound SQLite owner passes
both original rollback/connection-drop root-replacement tests unchanged. WAL,
writer-exclusion, and recovery fixtures have focused native macOS proof;
maintenance's attributed read-to-write race is fixed and the reservation test
passes 200 repetitions at that checkpoint. Catalog maintenance and reserved
fills now retain one root through inventory, publication, registration, and
owner release; publication leases survive explicit object/range maintenance.
Main-file replacement, other index owners, non-mutating inspection, physical
accounting, and native-platform acceptance remain explicit gates. Plan 018
keeps that unfinished work in the first safety
delivery cut; focused green tests do not establish full phase acceptance.

The integrated warm-read fixture also exposed Xet's detached cache-write race.
The shared hydrator now waits for operation-owned write attempts before success
and cancels pending puts on cancellation/drop. The original blocked-origin,
reopened-runtime fixture passes unchanged; cache failures remain best-effort.
Separate-process fetch/hydrate and provider acceptance are still required.
