# Implementation Plans

Generated by the improve skill on 2026-08-16. Execute each plan's internal
phases in order; independent plans may proceed in parallel unless dependencies
say otherwise. Each executor must read the selected plan fully, honor its STOP
conditions, and update its row when done.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 013 | Add Git protocol v2 and partial clone to Crab | P1 | XL | its internal Plan 012 prerequisite | TODO |
| 014 | Harden Crab CLI workflows into a safe DVC replacement | P0–P2 | XL | — | IN PROGRESS |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale)

## Dependency notes

- Plan 013 and Plan 014 affect different product boundaries and may proceed
  independently. Shared Git-ref, reader, or GC changes require explicit
  coordination before either plan edits the same contract.
- Plan 014 has internal cumulative gates G0–G6. Its phases must be executed in
  order even though the plan itself has no dependency on Plan 013.

## Findings considered and rejected

- A single “DVC compatibility” flag or fallback reader: rejected because it
  would preserve silent semantic loss and create two runtime paths instead of
  a verified migration boundary.
- Automatic `.dvc/` deletion after migration: rejected because Crab cannot
  safely remove the only recovery source; the user owns deletion after a
  machine-verifiable cutover report.
- Treating checkpoint outputs as persistent outputs: rejected because
  persistence has no lineage, apply, resume, metrics, transport, or GC
  semantics.
- Claiming all remote schemes are supported when only reads work: rejected;
  support is advertised per capability and requires live evidence.
