---
title: "crab ls-files"
description: "List files tracked by Crab with their sizes and chunk information."
meta:
  contentType: Reference
  goal: "Find the supported syntax and behavior for crab ls-files."
  audience: "Crab users who need exact command or configuration behavior."
---

# crab ls-files

List files tracked by Crab.

## Synopsis

```bash
crab ls-files [OPTIONS]
```

## Description

`crab ls-files` lists files managed by Crab's filter driver and reports their
hydration state. It currently lists the repository-wide set; use the display
options to control which fields are shown.

## Options

| Option | Default | Description |
|--------|---------|-------------|
| `-l, --long` | `false` | Show full 64-character hashes |
| `-s, --size` | `false` | Include file sizes |
| `-n, --name-only` | `false` | Show only file names |
| `-j, --json` | `false` | Emit machine-readable JSON output |
| `-d, --debug` | `false` | Show all available fields for debugging |

## Examples

### List all tracked files

```bash
crab ls-files
```

### List with hashes and sizes

```bash
crab ls-files --long --size
```

### Show only names

```bash
crab ls-files --name-only
```

Use `--name-only` as input to path-oriented shell tools, and use `--json` when
another program needs hashes, sizes, or hydration state. `--debug` exposes all
available fields and is intended for diagnosis rather than a stable display
contract. The command is read-only: it reports the repository-wide tracked set
without hydrating files or changing tracking patterns.

## Related commands

- [`crab status`](/docs/cli/reference/crab-status): inspect hydration state.
- [`crab track`](/docs/cli/reference/crab-track): manage tracking patterns.
- [`crab du`](/docs/cli/reference/crab-du): report storage usage.
