---
title: "crab fsck"
description: "Verify repository integrity by checking all references and stored objects."
meta:
  contentType: Reference
  goal: "Find the supported syntax and behavior for crab fsck."
  audience: "Crab users who need exact command or configuration behavior."
---

# crab fsck

Verify repository integrity.

## Synopsis

```bash
crab fsck [OPTIONS]
```

## Description

`crab fsck` performs a deep integrity check of the repository: verifying that
all pointer blobs reference valid chunks, all chunks are present in the remote
store, and all hashes match their content. It also reports expired resumable
multipart sessions recorded in the shared staging journal. `--repair` aborts
such a session only after reacquiring its lease and matching its exact provider
endpoint, container, and object key.

For conceptual background, see [Integrity Check](/docs/cli/diagnostics/integrity-check).

## Options

| Option | Default | Description |
|--------|---------|-------------|
| `--repair` | `false` | Attempt to repair detected issues |
| `--json` | `false` | Emit structured JSON output |
| `--jsonl` | `false` | Stream JSONL progress events |

## Examples

### Local integrity check

```bash
crab fsck
```

### Repair safe issues

```bash
crab fsck --repair
```

Run the read-only check first and preserve its output. `--repair` attempts only
the repairs implemented as safe by the installed release; it is not a request
to discard corrupt or missing referenced data. After any repair, run `crab
fsck` again without the flag and confirm a clean terminal result. Use JSONL for
long-running checks that need progress events and JSON for one final envelope.

## Related commands

- [`crab doctor`](/docs/cli/reference/crab-doctor): run broad health checks.
- [`crab gc`](/docs/cli/reference/crab-gc): garbage-collect unreachable objects.
