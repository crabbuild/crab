---
illustration_id: 08
type: infographic
style: blueprint
language: en
aspect_ratio: "16:9"
output: 08-continuity-aware-xorb-packing.png
---

# Continuity-Aware Xorb Packing

Use case: scientific-educational comparison infographic.

Primary request: completely redraw the reference image as a minimal two-column assignment table. Preserve only its blueprint palette. Discard its containers, arrows, balance scale, fact cards, icons, and detailed storage tracks.

Layout: one title line, then exactly two equal-width panels. Both panels share identical dimensions, padding, row heights, column widths, baselines, and stroke widths. All cells align to a strict six-column grid. No connector lines are needed.

PANEL TEMPLATE:
- Heading at one fixed baseline.
- Row label `File order` followed by six equal cells: `C1`, `C2`, `C3`, `C4`, `C5`, `C6`.
- Row label `Stored in` directly below, followed by six equal assignment cells aligned exactly under the file-order cells.
- One two-line result block below the rows.

LEFT PANEL:
- Heading: `REUSE-FIRST`.
- Stored-in row: `A`, `B`, `C`, `D`, `A`, `B`.
- Use amber for the assignment cells to emphasize scattering.
- Result: `Less upload` and `4 xorbs touched`.

RIGHT PANEL:
- Heading: `CONTINUITY-AWARE`.
- Stored-in row: `A`, `A`, `A`, `B`, `B`, `B`.
- Use engineering blue for the assignment cells to emphasize contiguous runs.
- Result: `Fewer reads` and `2 xorbs touched`.

FOOTER: one quiet centered line: `Same file order. Different storage locality.`

LABELS: Render only these labels, spelled exactly: `REUSE VS READ LOCALITY`, `REUSE-FIRST`, `CONTINUITY-AWARE`, `File order`, `Stored in`, `C1`, `C2`, `C3`, `C4`, `C5`, `C6`, `A`, `B`, `C`, `D`, `Less upload`, `4 xorbs touched`, `Fewer reads`, `2 xorbs touched`, `Same file order. Different storage locality.`.

COLORS: solid off-white background, deep slate outlines and text, amber left assignment row, engineering-blue right assignment row, pale blue file-order cells. Color values are rendering guidance only; never display color names or hex values.

STYLE: flat 2D minimal blueprint table; perfectly straight edges; six equal columns; precise row alignment; generous whitespace; large readable text. No perspective, shadows, gradients, icons, curves, arrows, connector lines, storage tracks, policy values, balance scales, floating labels, or extra text.

ASPECT: 16:9 landscape.
